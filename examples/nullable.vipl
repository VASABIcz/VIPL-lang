from out import print
from strings import *

fn rng(n: int): String? = n % 2 == 0 ? "UwU" : null

x = "OwO"

x = (rng 3) ?: "hello"

print x

fn hello(name: String?): String {
    return "hello " + (name ?: "null")
}

print(hello(null))