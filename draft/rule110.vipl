from out import print
from out import write
from utils import *

buf1 = makeArray(100)
buf2 = makeArray(100)

buf1[0] = 1

repeat i 100 {
    if i == 0 {
        continue
    }
    if i == 99 {
        continue
    }
    a = buf1[i-1]
    b = buf1[i]
    c = buf2[i+1]

    print "a"
}